angular
    .module('app')
    .component('wiHeader', {
        templateUrl: 'app/header/header.html',
        controller: wiHeaderController
    });

/** @ngInject */
function wiHeaderController() {
    
}
